# Security for artwork uploads
# Prevent PHP execution
<FilesMatch "\.php$">
    Require all denied
</FilesMatch>

# Deny hidden/dot-files
<FilesMatch "^\.">
    Require all denied
</FilesMatch>

# Prevent directory browsing
Options -Indexes